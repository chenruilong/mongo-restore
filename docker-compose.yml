services:
  app:
    platform: linux/amd64
    build: .
    ports:
      - "3456:3456"
    volumes:
      - backup-uploads:/tmp/backups
      - backup-data:/tmp/backup-data
    environment:
      - PORT=3456

volumes:
  backup-uploads:
  backup-data:
